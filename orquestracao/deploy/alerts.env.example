# Inject through Cloud Run environment/Secret Manager, not into the release ZIP.
# Configure only a corporate SMTP endpoint that permits authenticated STARTTLS.
# Rotate credentials previously exposed in chat; no real values belong here.
MONDAY_ALERT_SMTP_HOST=
EMAIL_REMETENTE=
EMAIL_SENHA_APP=
EMAIL_DESTINATARIOS=
