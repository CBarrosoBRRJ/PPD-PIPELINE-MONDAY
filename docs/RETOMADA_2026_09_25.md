# Retomada — 25/09/2026

## Estado de partida

V17 publicada pelo operador, execucao pipeline-monday-dtm7d com sucesso.
Agenda ENABLED, diariamente 06h America/Sao_Paulo. Ultimo recibo: SLA 1.363
projetos/6.366 passagens; fila 4; baixa qualidade 816. Soma 2.183 projetos
distintos sem duplicidades; destinations-control.json sem pending.
Isso nao comprova a proxima execucao automatica nem cobertura integral.

## Ordem de trabalho

1. Conferir execucao automatica de 25/09, logs vinculados ao execution_name,
   publicacoes verificadas, corte e capturas atualizados; nao executar manualmente
   antes de conferir se ha uma execucao em curso.
2. Validar os tres destinos com docs/QUERIES_ANALISE_VALIDACAO_SLA.md.
   Contagens podem mudar legitimamente: nao exigir os numeros de ontem.
3. PRIORIDADE DE NEGOCIO: investigar ausencia de passagens Globocorp no SLA.
4. Concluir Monitoring depois da liberacao de permissao, testar e confirmar
   recebimento por Caio E Cristina.
5. So apos esses recibos atualizar aceite, documentacao e GitHub. Nao declarar
   entrega integral nem continuidade entre ambientes homologada antecipadamente.

## Globocorp: evidencia e diferenca de conceitos

O usuario observou que monday_sla_orcamento so apresenta ambiente_origem=viu2.
Codigo conferido em destinations.py, reasons_for: qualquer mudanca de ambiente
entre passagens apos o primeiro status conhecido adiciona o motivo
continuidade_entre_ambientes_nao_homologada. Nao ha excecao por vinculo ja existente.
build envia o projeto inteiro para baixa qualidade se houver qualquer motivo.
A auditoria anterior registrou 213 projetos com esse motivo. Isso explica a
retirada das passagens Globocorp da populacao anterior; consultar a base atual
para confirmar. Nao significa que o vinculo ou o historico-fonte foi apagado.

Tres conceitos diferentes: identidade do projeto entre itens; evidencia da
transicao entre ambientes; duracao estimada em lacuna. Identidade e estimativa
nao comprovam sozinhas uma transicao observada. Por outro lado, excluir toda
troca de ambiente pode ser conservador demais: a implementacao atual nao
avalia individualmente se a continuidade ja foi comprovada.

Amanha: listar exemplos dos 213, mostrar marca/talento/nome/IDs dos dois itens,
cronologia e evidencias originais, distinguir lacuna real de bloqueio generico.
Verificar o mapa existente e eventual evidencia aprovada antes de propor nova
regra; nao refazer vinculos por nome isolado nem converter estimativa em observado.
Se mudar regra: testes de vinculo inequivoco/ambiguo, lacuna, sobreposicao e
reclassificacao diaria; medir impacto; publicar versao controlada com reconciliacao.
Nao exigir presenca em ambos ambientes de projetos que so existiram em um.

## Alertas: bloqueio concreto

plan apos apply: channels_to_create=0; policy_to_create=true. Os dois canais
foram criados, mas a politica nao. HTTP403 no POST da politica.
testIamPermissions retornou monitoring.alertPolicies.create,
logging.logEntries.create e serviceusage.services.use; nao retornou
logging.notificationRules.create. Usuario solicitou essa permissao ao responsavel.
Sem evidencia de concessao ainda. Job, tabelas e agenda nao foram alterados.

Depois da concessao:

```bash
python3 /home/caio_barroso/configure_monitoring_cloudshell.py plan
python3 /home/caio_barroso/configure_monitoring_cloudshell.py apply
```

Exigir configured_verified. Aguardar propagacao antes do teste:

```bash
python3 /home/caio_barroso/configure_monitoring_cloudshell.py test
```

test_log_written nao comprova entrega; ambos devem confirmar o e-mail.
Ver docs/ALERTAS_MONITORING.md. Alerta de erro nao detecta silencio total.
Credenciais antigas compartilhadas precisam de rotacao, sem reutiliza-las aqui.

## Primeira consulta operacional (somente leitura)

```bash
gcloud run jobs executions list \
  --job=pipeline-monday --project=gglobo-viu-dados-hdg-prd \
  --region=us-central1 --limit=5 \
  --format='table(metadata.name,status.startTime,status.completionTime,status.succeededCount,status.failedCount)'
```

Copiar o nome da execucao esperada para EXECUCAO abaixo:

```bash
EXECUCAO='SUBSTITUIR_PELO_NOME_DA_EXECUCAO'
gcloud logging read \
  "resource.type=\"cloud_run_job\" AND resource.labels.job_name=\"pipeline-monday\" AND labels.\"run.googleapis.com/execution_name\"=\"$EXECUCAO\" AND (jsonPayload.event=\"orchestration_end\" OR jsonPayload.event=\"consolidated_publication_confirmed\" OR severity>=ERROR)" \
  --project=gglobo-viu-dados-hdg-prd --freshness=48h \
  --order=asc --limit=50 --format='json(timestamp,severity,jsonPayload,textPayload)'
```

Sem execucao no horario, investigar Scheduler e autenticacao; nao concluir que
rodou apenas porque a agenda esta habilitada.
